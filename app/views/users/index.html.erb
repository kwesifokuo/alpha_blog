<h1 class="text-center mt-4 mb-2">Alpha Bloggers</h1>

<div class="flickr_pagination mb-2">
  <%= will_paginate @users, :container => false %>
</div>
<%= render 'user' %>
<div class="flickr_pagination mb-5">
  <%= will_paginate @users, :container => false %>
</div>